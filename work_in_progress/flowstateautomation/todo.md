# Next Steps (To Do)
- [ ] In n8n, go to the **Respond to Webhook (AI Reply)** node.
- [ ] Click the big orange **Execute previous nodes** button and send a test message on the live site to feed it data.
- [ ] Drag the AI's actual text response from the left pane into the `reply` quotes in the JSON box.
- [ ] Update the **AI Chat Response (OpenAI)** node's System Message to enforce the "Joe is in a build session" opening and strict Calendly push.
